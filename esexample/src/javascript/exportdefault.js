const welcome = "welcome";
const name = "mani";

export {welcome, name};

const add = (a, b) => a + b;
//const multiply = (c, d) => c * d;

export default (add);
//export default (multiply)// no need use curly braces in default